#include <stdio.h>
int main() {
    int a, b;
    
    /* সংখ্যা দুটা ইনপুট লোৱা */
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    
    /* যোগফল প্ৰিণ্ট কৰা */
    printf("Sum: %d\n", a + b);
    return 0;
}
